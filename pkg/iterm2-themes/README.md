# Spectre Pro themes

This directory contains the 607 iTerm2-compatible themes bundled with Spectre Pro.
The files are vendored into the repository so builds do not require a remote theme
archive or network access.

Theme files are configuration data loaded by Spectre Pro at runtime. They are
installed into `share/spectrepro/themes` by the build system.
